import 'package:flutter/material.dart';

class SoalPage extends StatelessWidget {
  const SoalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Soal (coming soon)'),
    );
  }
}
